const WFS_BASES=['https://geoserver.meioambiente.mg.gov.br/geoserver/IDE/ows','https://geoserver.meioambiente.mg.gov.br/IDE/ows','https://geoserver.meioambiente.mg.gov.br/geoserver/ows','https://geoserver.meioambiente.mg.gov.br/ows'];
const BBOX='-48.85,-19.35,-47.85,-18.45,EPSG:4326';
const KNOWN=['IDE:ide_210602_mg_imoveis_recomp_uso_restrito_declarado_car_pol'];
async function getFeature(base,typeName){const q=new URLSearchParams({service:'WFS',version:'1.0.0',request:'GetFeature',typeName,outputFormat:'application/json',srsName:'EPSG:4326',bbox:BBOX,maxFeatures:'5000'});const r=await fetch(`${base}?${q}`,{headers:{'user-agent':'MALHA/8.1'}});if(!r.ok)throw new Error(`${r.status}`);const j=JSON.parse(await r.text());if(j&&j.type==='FeatureCollection')return j;throw new Error('não GeoJSON')}
function onlyUberlandia(j){const features=(j.features||[]).filter(f=>{const p=f.properties||{},c=String(p.cod_imovel||p.COD_IMOVEL||'');const m=String(p.municipio||p.MUNICIPIO||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();return c.startsWith('MG-3170206-')||m==='uberlandia'});return {...j,features,totalFeatures:features.length,numberMatched:features.length,numberReturned:features.length}}
async function discover(base){const q=new URLSearchParams({service:'WFS',request:'GetCapabilities',version:'1.1.0'});const r=await fetch(`${base}?${q}`);if(!r.ok)return[];const x=await r.text();return[...x.matchAll(/<Name>([^<]+)<\/Name>/g)].map(m=>m[1]).filter(n=>/(car|sicar|imove.*rural|area_imovel)/i.test(n)).slice(0,30)}
export default async function handler(req,res){const errors=[];for(const base of WFS_BASES){for(const name of KNOWN){try{let j=onlyUberlandia(await getFeature(base,name));if(j.features.length){j.source='IEF/IDE-Sisema · dados declarados no CAR';j.layer=name;j.coverage='camada pública estadual derivada do CAR; filtrada para Uberlândia; cobertura parcial e não dominial';res.setHeader('Cache-Control','s-maxage=86400, stale-while-revalidate=604800');return res.status(200).json(j)}}catch(e){errors.push(`${name}:${e.message}`)}}try{for(const name of await discover(base)){try{let j=onlyUberlandia(await getFeature(base,name));if(j.features.length){j.source='IEF/IDE-Sisema · camada pública relacionada ao CAR';j.layer=name;j.coverage='camada pública relacionada ao CAR; filtrada para Uberlândia';res.setHeader('Cache-Control','s-maxage=86400, stale-while-revalidate=604800');return res.status(200).json(j)}}catch(e){errors.push(`${name}:${e.message}`)}}}catch(e){errors.push(e.message)}}res.status(502).json({type:'FeatureCollection',features:[],error:'CAR público não respondeu',detail:errors.slice(-8)})}
